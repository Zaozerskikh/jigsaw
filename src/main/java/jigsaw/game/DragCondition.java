package jigsaw.game;

/**
 * Enum that stores Drag conditions
 * (MOVE - when dragged figure is moved to cell,
 * REMOVE - dragged when figure is removed from cell,
 * PUT - when dragged figure is put into cell).
 */
public enum DragCondition {
    MOVE,
    REMOVE,
    PUT,
}
